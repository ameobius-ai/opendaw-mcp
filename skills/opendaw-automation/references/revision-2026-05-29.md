# openDAW MCP Server Revision — 2026-05-29

## 4 Critical Bugs Found & Fixed

### 1. `get_project_state` completely broken
- Lines 228-248: truncated code with literal `[OUTPUT TRUNCATED]` baked into the file
- Agent wrote truncated tool output directly into source code
- Fix: rewrote from scratch with proper audio unit enumeration

### 2. `unit.box` instead of `auBox` in forEach
- Loop variable was `auBox` but body referenced undefined `unit`
- Caused silent failures when iterating audio units for project state
- Fix: standardized to `au` as loop variable name

### 3. Missing tools (5 new tools added)
- `create_audio_track` — create audio track on primary AU
- `create_note_track` — create MIDI/note track on primary AU
- `set_track_volume` — per-track volume (dB)
- `set_track_panning` — per-track pan (-1.0 to 1.0)
- `set_track_mute` — per-track mute toggle
These were listed in the skill but not implemented in server.py.

### 4. JS injection in string params
- `effect_type` and `filename` were interpolated into JS without sanitization
- Fix: whitelist for effect names (checked against `DAW_EffectFactories`), regex sanitizer for filenames

## Post-Revision State
- 19 MCP tools total, 837 lines, 0 bugs
- Vite on :5174, Python venv functional
- Production file: `~/projects/creative-studio/agent-daw/opendaw-mcp/server.py`

## Verified Tool Inventory (19 tools)
```
Transport:   transport, set_position, set_bpm
Tracks:      create_audio_track, create_note_track
Audio:       load_audio, place_audio_region
Mixer:       set_track_volume, set_track_panning, set_track_mute, set_track_solo
Effects:     list_effects, add_effect
MIDI:        create_note
Export:       export_mix
State:       get_project_state, serialize
Edit:        undo, redo
```
